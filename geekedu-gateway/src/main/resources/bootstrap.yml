server:
  port: 80
spring:
  application:
    name: gateway-service

  cloud:
    gateway:
      enabled: true
      routes:
        - id: label-service-route
          uri: http://localhost:8086
          predicates:
            - Path=/label
